Here is the **exact request flow** for your setup (Kubernetes + Ingress + NodePort + IP access without domain):

---

# 🔥 **REQUEST FLOW (Without Domain – IP + Ingress Port)**

When you call Postman:

```
http://159.89.172.251:30080/student/api/users/all-users
```

The flow is:

```
Postman
   ↓
IP:30080  (Ingress NodePort)
   ↓
Ingress Controller (nginx-ingress)
   ↓
Ingress Rule (path "/")
   ↓
Service: icsquiz-user-service (ClusterIP)
   ↓
TargetPort: 3090
   ↓
Pod (icsquiz-user-app)
   ↓
Controller method → Return response
```

---

# 🌐 **1️⃣ Request Enters NodePort**

You used custom ingress ports:

* HTTP → `30080`
* HTTPS → `30443`

So the incoming request hits:

```
Ingress Service: ingress-nginx-controller
```

---

# 🚦 **2️⃣ Ingress Controller Routes Request**

Your Ingress Rule (no host):

```yaml
rules:
  - http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: icsquiz-user-service
              port:
                number: 3090
```

Meaning:

✔ ANY request → `/` → forward
✔ To service `icsquiz-user-service`
✔ On port `3090`

---

# 🔁 **3️⃣ Service Finds Matching Pods**

Your service YAML (NodePort 30090) forwards traffic to:

```
Pods with label app=icsquiz-user-app
```

Because service spec says:

```yaml
selector:
  app: icsquiz-user-app
```

---

# 🧩 **4️⃣ Pod Receives Request**

Pod container listens on:

```
Server port: 3090
```

So final route:

```
Ingress → Service → Pod:3090 → Spring Boot Controller
```

---

# 🎉 **5️⃣ Spring Boot Returns Response**

Your controller method:

```
/student/api/users/all-users
```

Responds and sends data back.

---

# 📦 **FULL REQUEST FLOW DIAGRAM**

```
POSTMAN
   ↓
http://159.89.172.251:30080/student/api/users/all-users
   ↓
[NodePort 30080]  Ingress Service
   ↓
NGINX Ingress Controller (ingress-nginx)
   ↓
Ingress Rule (path / → service icsquiz-user-service)
   ↓
ClusterIP Service: icsquiz-user-service
   ↓
TargetPort: 3090
   ↓
Pod (icsquiz-user-app)
   ↓
StudentController → return users
   ↓
Response goes back same route
```

---

# 🔥 If you want, I can also draw this as a **PNG diagram**

Just tell me:

👉 **“Give diagram PNG”**

Or if you want the React + backend flow:

👉 **“Give React + backend ingress flow”**
